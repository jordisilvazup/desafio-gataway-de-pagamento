version: '3'
services:
  postgres:
    image: 'postgres:alpine'
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: pagamento

  redis:
    container_name: reddis
    image: 'redis:latest'
    ports:
      - 6379:6379